(0,0) min_diff = float("inf") for x in range(n): for y in range(x+1, n): diff = abs(arr[x] - arr[y]) if diff < min_diff: min_diff = diff pair = (arr[x], arr[y]) return pair Next, propose a separate algorithm (in Python) for a sorted list of integers to achieve the same goal. [10