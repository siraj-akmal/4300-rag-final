qty: { $lt: 30 } }, which corresponds to the following SQL statement: See for other MongoDB comparison operators. Specify OR Conditions Using the operator, you can specify a compound query that joins each clause with a logical OR conjunction so that the query selects the documents in the collection