to iterate through documents in a cursor, refer to your driver's documentation. If you are using mongosh, see Additional Methods and Options The following can also read documents from a collection: Collection.findOne() In , the pipeline stage provides access to MongoDB queries. See the MongoDB Node.js Driver's aggregation tutorial. Read