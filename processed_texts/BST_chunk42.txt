largest) key that belongs to the tree and that is strictly greater than (respectively, less than) k. The idea for ﬁnding the successor of a given node x. • If x has the right child, then the successor is the minimum in the right subtree of x. • Otherwise, the