sorted list of integers to achieve the same goal. [10 points] def closest_pair_sorted(arr): n = len(arr) pair = (0,0) min_diff = float('inf') for x in range(n-1): diff = abs(arr[x] - arr[x+1]) if diff < min_diff: min_diff = diff pair = (arr[x], arr[x+1]) return pair Briefly discuss which algorithm is more