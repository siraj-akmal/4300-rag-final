nil do y ←right[y] 4: return (key[y]) 10 3. Insertion Suppose that we need to insert a node z such that k = key[z]. Using binary search we ﬁnd a nil such that replacing it by z does not break the BST-property. 11 BST-Insert(x, z, k) 1: if x =