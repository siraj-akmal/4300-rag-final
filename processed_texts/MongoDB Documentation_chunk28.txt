documents from the inventory collection where status equals either "A" or "D":   const cursor = db.collection('inventory Node.js   SELECT * FROM inventory WHERE statu NOTE The MongoDB Compass query bar autocompletes the current query based on the keys in your collection's documents, including keys in embedded sub-documents.