keys in your collection's documents, including keys in embedded sub-documents. query filter document query operators   { <field1>: { <operator1>: <value1> }, Node.js 3/17/25, 9:55 AM Query Documents - MongoDB Manual v8.0 - MongoDB Docs https://www.mongodb.com/docs/manual/tutorial/query-documents/ 5/14 The operation uses a query predicate of { status: { $in: [ "A", "D" ] } }, which corresponds to the following SQL statement: Refer to the document for the complete list of MongoDB query operators. Specify AND Conditions A compound query can specify conditions for more than one field in the collection's documents. Implicitly, a logical AND conjunction connects the clauses of a compound query so that the query selects the documents in the collection that match all the conditions.   const cursor = db.collection('inventory status: { $in: ['A', 'D'] } }); Node.js NOTE Although you can express this query using the operator, use the operator rather than the operator when performing equality checks on the same field. $or $in $or   SELECT * FROM inventory WHERE statu Query and Projection Operators 3/17/25, 9:55 AM Query Documents - MongoDB Manual v8.0 - MongoDB Docs https://www.mongodb.com/docs/manual/tutorial/query-documents/ 6/14 The following example retrieves all documents in the inventory collection where the status