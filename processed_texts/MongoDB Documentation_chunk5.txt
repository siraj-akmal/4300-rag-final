when performing equality checks on the same field. $or $in $or   SELECT * FROM inventory WHERE statu Query and Projection Operators 3/17/25, 9:55 AM Query Documents - MongoDB Manual v8.0 - MongoDB Docs https://www.mongodb.com/docs/manual/tutorial/query-documents/ 6/14 The following example retrieves all documents in the inventory collection where the status equals "A" and qty is less than ( ) 30: The operation uses a query predicate of { status: "A", qty: { $lt: 30 } }, which corresponds to the following SQL statement: See for other MongoDB comparison operators. Specify OR Conditions Using the operator, you can specify a compound query that joins each clause with a logical OR conjunction so that the query selects the documents in the collection that match at least one condition. The following example retrieves all documents in the collection where the status equals "A" or qty is less than ( ) 30: $lt   const cursor = db.collection('inventory status: 'A', qty: { $lt: 30 } }); Node.js   SELECT * FROM inventory WHERE statu comparison operators $or $lt 3/17/25, 9:55 AM Query Documents - MongoDB Manual v8.0 - MongoDB Docs https://www.mongodb.com/docs/manual/tutorial/query-documents/ 7/14 The operation uses a query predicate of { $or: [