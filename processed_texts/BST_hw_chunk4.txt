the two integers. Your algorithm should not first sort the list. [10 points] def closest_pair(arr): n = len(arr) pair = (0,0) min_diff = float("inf") for x in range(n): for y in range(x+1, n): diff = abs(arr[x] - arr[y]) if diff < min_diff: min_diff = diff pair = (arr[x], arr[y]) return