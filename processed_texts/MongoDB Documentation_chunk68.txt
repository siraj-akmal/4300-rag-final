where the year field matches 1924. Additional Query Tutorials For additional query examples, see: Behavior Cursor The Collection.find() method returns a cursor. Concurrent Updates While Using a Cursor As a cursor returns documents, other operations may run in the background and affect the results, depending on the read concern level.